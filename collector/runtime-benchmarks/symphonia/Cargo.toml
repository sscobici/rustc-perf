[package]
name = "symphonia-bench"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
benchlib = { path = "../../benchlib" }
symphonia = { path = "../../../../Symphonia/symphonia", features = ["all", "opt-simd"] }

[workspace]
